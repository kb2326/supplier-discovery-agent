# Supplier-discovery demo — minimal dependency set.
# All "interesting" logic runs on these; nothing here phones home at runtime.
fastapi==0.136.3
uvicorn==0.49.0
pydantic==2.13.4
scikit-learn==1.9.0     # TF-IDF semantic-ish product matching (no model downloads)
rapidfuzz==3.14.5       # entity resolution + fuzzy name matching
anthropic==0.105.2      # OPTIONAL: query parsing only; deterministic fallback works offline
